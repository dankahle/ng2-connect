/*
import {bootstrap, Component, EventEmitter, CORE_DIRECTIVES, FORM_DIRECTIVES} from 'angular2/angular2';
import {Parent} from './comp/nestedEvents/parent';

@Component({
   selector: 'my-app',
//   templateUrl: 'app/app.html',
   template: '<div (click)="handler($event)" parent class="shit">lala</div>',
   directives: [CORE_DIRECTIVES, FORM_DIRECTIVES, Parent]
})

export class AppComponent {
   handler(v) {
      console.log('app', v);
   }
}
bootstrap(AppComponent);


*/
